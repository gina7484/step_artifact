# step_tl

* To install required packages: use the `yml` file. We have no_build version for cross-platform support.
* To write a STep program in Python and generate the npy file and protobuf file, look into the tests in `tests` repo.
* If you run into error such as `cannot find step_py`, run `source setup.sh`.
